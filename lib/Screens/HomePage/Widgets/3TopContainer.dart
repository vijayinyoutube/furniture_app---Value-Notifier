import 'package:flutter/material.dart';

Widget buildTopContainer(BuildContext context) => Container(
      width: MediaQuery.of(context).size.width,
      height: 150,
      color: Colors.transparent,
    );
